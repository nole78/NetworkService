<UserControl x:Class="NetworkService.Views.TerminalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NetworkService.Views"
             xmlns:viewmodels="clr-namespace:NetworkService.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="800">
    
    <UserControl.DataContext>
        <viewmodels:TerminalViewModel/>
    </UserControl.DataContext>
    
    <Canvas Background="Black" Width="800" Height="250">
        <Grid Width="800" Height="250">
            <Grid.RowDefinitions>
                <RowDefinition Height="20"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock Text="Renewable_scada_terminal" TextAlignment="Center" Background="DarkGray" FontSize="14" FontWeight="Bold" VerticalAlignment="Top"/>

            <StackPanel Grid.Row="1">
                <ListView ItemsSource="{Binding TerminalLines, UpdateSourceTrigger=PropertyChanged}" 
                          MaxHeight="210" 
                          Background="Black"
                          BorderThickness="0">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="IsHitTestVisible" Value="False"/>
                            <Setter Property="Focusable" Value="False"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Text}" FontSize="13" FontFamily="Consolas">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="White"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Type}" Value="Command">
                                                <Setter Property="Foreground" Value="Cyan"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Type}" Value="Error">
                                                <Setter Property="Foreground" Value="Red"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Type}" Value="Success">
                                                <Setter Property="Foreground" Value="LimeGreen"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <StackPanel Orientation="Horizontal">
                    <Label Content="renewable__scada__terminal>"
                           Foreground="Cyan"
                           FontSize="13"
                           FontFamily="Consolas"
                           VerticalContentAlignment="Center"/>
                    <TextBox Text="{Binding  Command, UpdateSourceTrigger=PropertyChanged}" 
                             Background="Transparent" 
                             Foreground="Cyan"
                             VerticalAlignment="Center"
                             BorderThickness="0" 
                             FontSize="13" 
                             FontFamily="Consolas"
                             Width="618">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding TerminalCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Canvas>
</UserControl>
