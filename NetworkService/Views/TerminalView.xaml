<UserControl x:Class="NetworkService.Views.TerminalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NetworkService.Views"
             xmlns:viewmodels="clr-namespace:NetworkService.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="180" d:DesignWidth="800">
    
    <UserControl.DataContext>
        <viewmodels:TerminalViewModel/>
    </UserControl.DataContext>

    <Grid Width="800" Height="180" 
          Background="Black"
          FocusManager.FocusedElement="{Binding ElementName=TerminalInput}" >
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Text="Renewable_scada_terminal" TextAlignment="Center" Background="DarkGray" FontSize="15" FontWeight="Bold" VerticalAlignment="Top"
                   FontFamily="Calibri" />

        <StackPanel Grid.Row="1">
            <ListView ItemsSource="{Binding TerminalLines, UpdateSourceTrigger=PropertyChanged}" 
                          MaxHeight="140" 
                          Background="Black"
                          BorderThickness="0">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="IsHitTestVisible" Value="False"/>
                        <Setter Property="Focusable" Value="False"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Text}" FontSize="13" FontFamily="Consolas">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="White"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Type}" Value="Command">
                                            <Setter Property="Foreground" Value="Cyan"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Type}" Value="Error">
                                            <Setter Property="Foreground" Value="Red"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Type}" Value="Success">
                                            <Setter Property="Foreground" Value="LimeGreen"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Type}" Value="Question">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <StackPanel Orientation="Horizontal">
                <Label Content="renewable__scada__terminal>"
                           Foreground="Cyan"
                           FontSize="13"
                           FontFamily="Consolas"
                           VerticalContentAlignment="Center"/>
                <TextBox Text="{Binding  Command, UpdateSourceTrigger=PropertyChanged}" 
                         Name="TerminalInput"
                         Background="Transparent" 
                         Foreground="Cyan"
                         VerticalAlignment="Center"
                         BorderThickness="0" 
                         FontSize="13" 
                         FontFamily="Consolas"
                         Width="618"
                         CaretBrush="White">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding ExecuteTerminalCommand}"/>
                        <KeyBinding Key="Up" Command="{Binding GetOlderCommand}"/>
                        <KeyBinding Key="Down" Command="{Binding GetNewerCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
